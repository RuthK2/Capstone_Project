{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd expense_tracker && python manage.py migrate && gunicorn expense_tracker.wsgi:application --bind 0.0.0.0:$PORT"
  }
}