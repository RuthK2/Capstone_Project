{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate --run-syncdb --verbosity=2 && python manage.py migrate --verbosity=2 && gunicorn expense_tracker.expense_tracker.wsgi:application --bind 0.0.0.0:$PORT"
  }
}